rootProject.name = "db-ratchet"

include "core"
include "cmdline"
include "gradle-plugin"

rootProject.children.each { childProject ->
    childProject.name = "${rootProject.name}-${childProject.name}"
}
